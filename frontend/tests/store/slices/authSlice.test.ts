// Auth slice test
// import authReducer from '../../app/store/slices/authSlice';
// test('auth reducer initial state', () => {
//   expect(authReducer(undefined, { type: '' })).toEqual({ user: null, token: null, role: null });
// });
